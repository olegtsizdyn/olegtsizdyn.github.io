export const defaultConfigFullPage = {
  activeClass:          'active', // the class that is appended to the sections links
  anchors:              ['welcome', 'about', 'skills', 'examples', 'contacts'], // the anchors for each sections
  arrowNavigation:      true, // use arrow keys
  className:            `container_dark`, // the class name for the section container
  delay:                700, // the scroll animation speed
  navigation:           true, // use dots navigatio
  scrollBar:            false, // use the browser default scrollbar
  sectionClassName:     'container_section', // the section class name
  sectionPaddingTop:    '0', // the section top padding
  sectionPaddingBottom: '0', // the section bottom padding
  verticalAlign:        false // align the content of each section vertical
}